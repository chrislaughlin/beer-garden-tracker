import { BelfastMapPanel } from '@/components/maps/belfast-map-panel';

export function MapPreviewCard() {
  return <BelfastMapPanel title="Nearby beer gardens around central Belfast" description="Map preview for discovery, radius search, and map/list switching on mobile." />;
}
